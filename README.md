# herbature
Un 'faux' site web de vente en ligne de gazon


Le site ne fonctionne pas directement dans GitHub il faut télécharger les fichier (le ZIP depuis GitHub) puis l'extraire et le lancer dans un éditeur de code. Ensuit il faux vérivier que tout les importation/librairie son bien instaler (sqlite3, hashlib, datetime,...). Il est maintenant temps de lancer le tout, pour sa on run l'initialisation de la base de données (dans database le fichier Init_BDD.py) puis app.py (dans la racine) on suivre alors les instructions (ctrl + clike sur http://127.0.0.1:5000) et après en principe tout fonctionne :)
